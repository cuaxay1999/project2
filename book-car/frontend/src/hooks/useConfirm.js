import React, { useState } from 'react';
import { Icon, Input, Confirm, Modal, Button } from 'semantic-ui-react';

const useConfirm = () => {
    const [confirmStatus, setConfirmStatus] = useState(false);
    return <div></div>;
};

export default useConfirm;
